//- Created by Rohden on 22/01/2017
extends globals

block html
    doctype html

    html(lang="en")
    head
      meta(charset="UTF-8")
      title Hour input demo

      +importBootStrap

      +importAngular
      script(type="text/javascript" src="../build/grHourInput.min.js")
      script(type="text/javascript" src="./app/app.js")

    body(data-ng-app="Demo" data-ng-controller="demoController")
      div.container
        div.page-header
          h1 Time input converter and filters
          h1
            small Format input will only convert on
              strong  blur
        div.row.alert.alert-info
          div.col-md-12.col-xs-2
              | Since the beggining of the day has been passed
              strong  {{currentDayMillis}}
              |  milliseconds, wich converted are equivalent to
              strong  {{ currentDayMillis | floatToHourFormat :{expected: 'milliseconds', minString: 'the beginning of the day'} }}
        div.row
          div.col-md-12.col-xs-12.form-group
            h3 Type the desired hour (eg: 1.5, 2:50,1,3, 0h 30m, etc):
        div.row
          div.col-md-2.col-xs-4.form-group
            gr-hour-input.form-control(ng-model="hourString")
          div.col-md-10.col-xs-8
            h4
              | which is
              strong  {{ hourString }}
              |  or
              strong  {{ hourString | formatToMillis }}
              |  milliseconds
        div.row
          div.col-md-12.col-xs-12.form-group
            h3 Or you can use milliseconds:
        div.row
          div.col-md-2.col-xs-4.form-group
            input.form-control(data-ng-model="milliSeconds" type="number" step=10000)
          div.col-md-10.col-xs-8.form-group
            h4 milliseconds is equivalent to
              strong  {{ milliSeconds | floatToHourFormat :{expected: 'milliseconds', minString: 'custom string for zero'} }}
        div.row
          h4
            | Source code can be found at:
            a(href="https://github.com/Grohden/grHourInput")   https://github.com/Grohden/grHourInput